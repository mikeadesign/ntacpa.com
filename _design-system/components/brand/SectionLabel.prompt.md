Use this to open a section — the tracked uppercase eyebrow that appears above every section heading in NTA collateral.

```jsx
<SectionLabel number="03">Services</SectionLabel>
<SectionLabel tone="inverse">Contact</SectionLabel>
```

Numbering is optional but consistent within a page: number every section or none.
Use `tone="inverse"` on navy fields.
