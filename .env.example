# Where the contact form POSTs. Until this is set, the form refuses to claim
# it sent anything and shows a panel pointing at the phone number and email
# instead. Any service that accepts a multipart POST and returns 2xx works
# (Formspree, Netlify Forms, a Cloudflare Worker, a mail function).
PUBLIC_CONTACT_ENDPOINT=
